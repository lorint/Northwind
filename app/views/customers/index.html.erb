<script src="http://code.jquery.com/ui/1.11.1/jquery-ui.js"></script>

<link rel="stylesheet" href="http://code.jquery.com/ui/1.11.1/themes/smoothness/jquery-ui.css">

<!-- <div id="resizable" style="position: absolute; width:100px; height:100px; border:3px solid gray">Hello world!</div>
<div id="milk" style="position: absolute; width:100px; height:100px; border:3px solid gray">Hello world!</div>
<div id="eggs" style="position: absolute; width:100px; height:100px; border:3px solid gray">Hello world!</div>
<div id="beer" style="position: absolute; width:100px; height:100px; border:3px solid gray">Hello world!</div>

<div id="droppable" style="width:200px; height:200px; border:3px solid gray">Hello world!</div>
 -->
<script>
  $("#resizable").draggable().resizable();
  $("#milk").draggable().resizable();
  $("#eggs").draggable().resizable();
  $("#beer").draggable().resizable();
  $( "#droppable" ).droppable({
    drop: function( event, ui ) {
      console.log(ui.draggable);
    }
  });
</script>

<h1>Listing customers' info</h1>

<table>
  <thead>
    <tr>
      <th>Company name</th>
      <th>Contact name</th>
      <th>Contact title</th>
      <th>Address</th>
      <th>City</th>
      <th>Region</th>
      <th>Postal code</th>
      <th>Country</th>
      <th>Phone</th>
      <th>Fax</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td><%= customer.company_name %></td>
        <td><%= customer.contact_name %></td>
        <td><%= customer.contact_title %></td>
        <td><%= customer.address %></td>
        <td><%= customer.city %></td>
        <td><%= customer.region %></td>
        <td><%= customer.postal_code %></td>
        <td><%= customer.country %></td>
        <td><%= customer.phone %></td>
        <td><%= customer.fax %></td>
        <td><%= link_to 'Orders', orders_path(customer_id: customer.id) %></td>
        <td><%= link_to 'Edit', edit_customer_path(customer) %></td>
        <td><%= link_to 'Destroy', customer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Customer', new_customer_path %><br />
<br />
<%= link_to 'Employees', employees_path %><br />
<%= link_to 'Product Categories', categories_path %><br />
<%= link_to 'Products', products_path %><br />
